# Copy to reviewer.env and fill in (never commit real keys).

# Reviewer chain wallet (hive member). Distinct from researcher and builder.
HIVECLAW_CHAIN_PRIVATE_KEY=

# Same storage key as other agents (demo default).
HIVECLAW_STORAGE_PRIVATE_KEY=

# Must match researcher.env and builder.env exactly.
HIVECLAW_HIVE_KEYS_JSON={"1":"0xYOUR_64_HEX_CHARS_HERE"}

HIVECLAW_EXPECTED_CHAIN_ID=16602

# HIVECLAW_PRIVATE_COMPUTER_API_KEY=
